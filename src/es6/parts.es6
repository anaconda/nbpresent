const PART = {
  source: "source",
  outputs: "outputs",
  widgets: "widgets"
};

const PART_SELECT = {
  source: ".input .inner_cell .input_area",
  outputs: ".output_wrapper .output",
  widgets: ".widget-area .widget-subarea"
};

export {PART, PART_SELECT};
