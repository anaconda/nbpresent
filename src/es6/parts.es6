const PART = {
  source: "source",
  outputs: "outputs",
  widgets: "widgets"
};

const PART_SELECT = {
  source: ".inner_cell",
  outputs: ".output_wrapper",
  widgets: ".widget-area .widget-subarea"
};

export {PART, PART_SELECT};
